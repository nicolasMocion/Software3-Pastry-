{
  "name": "backend-gateway",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "db:seed": "node ./src/config/db.js",
    "db:reset": "npm run db:drop && npm run db:create && npm run db:import && npm run db:seed",
    "db:import": "psql -U pastry_db -d pastry_db -f database/schemas/DiagramaPasteleria.sql",
    "db:create": "createdb -U postgres pastry_db",
    "db:drop": "dropdb -U postgres --if-exists pastry_db"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "16.4.5",
    "express": "^4.21.2",
    "morgan": "1.10.1",
    "pg": "8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "nodemon": "3.1.10"
  }
}
